/**
 * Re-export dental branding for convenience.
 * Full branding (2 fonts, 2 colors, 1 gradient, max width, creative radius) lives in ./config.
 */
export { DENTAL_RADIUS, DENTAL_MAX_WIDTH, DENTAL_FONT_HEADING, DENTAL_FONT_BODY } from "./config";
